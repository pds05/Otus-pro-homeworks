spring:
  application:
    name: producer
  activemq:
    broker-url: 'tcp://localhost:61616'
    user: user
    password: rmohr
  jms:
    template:
      default-destination: 'message.queue'
logging:
  level:
    org.apache.activemq: DEBUG

